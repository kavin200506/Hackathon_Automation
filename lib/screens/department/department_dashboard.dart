import 'package:flutter/material.dart';

class DepartmentDashboardScreen extends StatelessWidget {
  const DepartmentDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Department Dashboard')),
      body: const Center(child: Text('Department Dashboard - To be implemented')),
    );
  }
}






