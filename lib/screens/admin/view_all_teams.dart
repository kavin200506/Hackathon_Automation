import 'package:flutter/material.dart';

class AdminViewAllTeamsScreen extends StatelessWidget {
  const AdminViewAllTeamsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('View All Teams')),
      body: const Center(child: Text('Admin View All Teams - To be implemented')),
    );
  }
}






