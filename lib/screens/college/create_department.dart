import 'package:flutter/material.dart';
import '../../../core/theme.dart';

class CreateDepartmentScreen extends StatelessWidget {
  const CreateDepartmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Department')),
      body: const Center(child: Text('Create Department - To be implemented')),
    );
  }
}

