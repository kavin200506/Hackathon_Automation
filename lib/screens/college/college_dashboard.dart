import 'package:flutter/material.dart';
import '../../../core/theme.dart';

class CollegeDashboardScreen extends StatelessWidget {
  const CollegeDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('College Dashboard')),
      body: const Center(child: Text('College Dashboard - To be implemented')),
    );
  }
}

